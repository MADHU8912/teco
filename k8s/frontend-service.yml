apiVersion: v1
kind: Service

metadata:
  name: teco-frontend-service

spec:
  type: NodePort

  selector:
    app: teco-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000