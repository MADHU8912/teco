apiVersion: v1
kind: Service

metadata:
  name: teco-backend-service

spec:
  type: NodePort

  selector:
    app: teco-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30001